package oncall.domain.data

object Holiday {
    val holidays = listOf(
        Month(1) to 1,
        Month(3) to 1,
        Month(5) to 5,
        Month(6) to 6,
        Month(8) to 15,
        Month(10) to 3,
        Month(10) to 9,
        Month(12) to 25,
    )
}